def travelPlces():
# input is a built-in function that allows us to pass in data 
# from the terminal and assign it ot a varible

    # built-in function - a function thats built into the programing
    # language that we dont need to write instructions for.
    # the instruction are already written behind the scenes, we just 
    # need to call it 
     
    place = input('where have you traveled to?')
    # When we answer the question above, the data we give the program
    # will be assigned to the variable  
    print(place)              